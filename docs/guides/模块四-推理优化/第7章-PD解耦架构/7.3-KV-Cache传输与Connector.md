---
title: "7.3 KV Cache 传输与 Connector：搬走一座临时仓库"
description: "手算 GQA 模型 KV Cache 大小与带宽需求，理解 Connector、NIXL、NCCL、LMCache 和传输边界"
pubDate: 2026-08-12
category: "inference-optimization"
chapter: 7
order: 703
tags: ["KV Cache", "KV Connector", "NIXL", "NCCL", "LMCache", "RDMA"]
---

## 📑 本节目标

P/D 解耦最容易被低估的一步， 是把 Prefill 产生的 KV Cache 送到 Decode。 它不是几十字节的请求元数据， 而可能是每个请求数百 MiB 甚至数 GiB 的 Tensor。 本节完成四件事：

- 从模型配置推导 KV 字节数。
- 把网络标称 Gbps 换成传输毫秒。
- 区分 Connector、传输后端和 KV 存储层。
- 建立可核实的带宽测试与故障检查方法。

## 1. 类比：不是递交票据，而是搬仓库

Prefill 结束时， 首 token 只是柜台上的一张提货单。 真正让 Decode 能继续工作的， 是每一层、每个 Prompt token 对应的 K 和 V。 若把请求看成搬家：

- 请求 ID 是快递单号。
- block table 是箱子清单。
- KV Tensor 是箱子里的物品。
- Connector 是物流接口。
- UCX/NCCL 等是公路或铁路。
- HBM、DRAM、NVMe 是仓库。

只优化“下单 API”， 并不会让一吨货物瞬间到达。

## 2. KV Cache 大小公式

先考虑常见的 Transformer 自注意力。 每个 token、每一层都保存 Key 和 Value。 记：

- \(L\)：Transformer 层数。
- \(H_{kv}\)：KV Head 数。
- \(D_h\)：每个 Head 的维度。
- \(S\)：每个元素字节数。
- \(T\)：需要传输的 token 数。
- 因子 2：分别对应 K 和 V。

则总 KV 大小近似为：

\[
Bytes_{KV}=2 \times L \times H_{kv} \times D_h \times S \times T
\]

每 token 大小为：

\[
Bytes_{token}=2 \times L \times H_{kv} \times D_h \times S
\]

这是数据主体的近似值， 未包含 block 对齐、元数据、传输协议缓冲和临时副本。

## 3. MHA、GQA 与 MQA 的区别

在传统 Multi-Head Attention 中， \(H_{kv}=H_q\)。 在 Grouped-Query Attention 中， 多个 Query Head 共享一组 K/V， 所以 \(H_{kv}<H_q\)。 在 Multi-Query Attention 中， \(H_{kv}\) 甚至可以为 1。 因此不能用 `num_attention_heads` 直接计算所有模型。 应读取配置里的 `num_key_value_heads`。 可核实命令：

```bash
python - <<'PY'
from transformers import AutoConfig

model = "meta-llama/Llama-3.1-8B-Instruct"
c = AutoConfig.from_pretrained(model)
kv_heads = getattr(c, "num_key_value_heads", c.num_attention_heads)
head_dim = getattr(c, "head_dim", c.hidden_size // c.num_attention_heads)
print({
    "layers": c.num_hidden_layers,
    "attention_heads": c.num_attention_heads,
    "kv_heads": kv_heads,
    "head_dim": head_dim,
})
PY
```

模型仓库可能执行自定义配置代码。 对不可信仓库，不要轻易增加 `trust_remote_code=True`。

## 4. 手算：Llama 3.1 8B 的 8K Prompt

采用公开配置中的典型参数：

- 层数 \(L=32\)。
- KV Head 数 \(H_{kv}=8\)。
- Head Dimension \(D_h=128\)。
- BF16/FP16 元素 \(S=2\) bytes。

每 token：

\[
2 \times 32 \times 8 \times 128 \times 2 =131072\text{ bytes} =128\text{ KiB}
\]

8K 若按 8192 token：

\[
128\text{ KiB}\times8192 =1,073,741,824\text{ bytes} =1\text{ GiB}
\]

也就是说， 一个 8K Prompt 的 KV 主体约 1 GiB。 若是 32K Prompt：

\[
1\text{ GiB}\times4=4\text{ GiB}
\]

这还只是一个请求。 每秒进入 10 个 8K Prompt， 数据生成率近似就是 10 GiB/s。

## 5. KV 量化怎样影响传输

若 KV dtype 从 2 bytes 降到 1 byte， 主体大小理论上减半。 同一例子中：

- BF16/FP16：8K Prompt 约 1 GiB。
- FP8 KV：8K Prompt 约 0.5 GiB。

但必须同时确认：

- P 和 D 是否使用相同 KV dtype。
- Connector 是否支持该 dtype。
- 目标 Attention backend 是否支持。
- 量化 scale 是否需要额外传输。
- 精度是否通过目标任务评测。

“网络省一半”不能跳过兼容性和质量验证。

## 6. Tensor Parallel 下怎么算

若 KV heads 在 \(TP\) 个 rank 间均匀切分， 每个 rank 持有的数据近似是总量的 \(1/TP\)。 但所有 rank 的聚合传输量仍接近总 KV 大小。 例如 1 GiB KV、TP=4：

- 每个 rank 约发送 256 MiB。
- 四个 rank 合计约 1 GiB。

若四张 GPU 各有独立、无争用的链路， 传输可并行。 若最终共享一个 NIC 或 PCIe 上行， 瓶颈仍是共享链路的聚合带宽。 不要把“单 rank 字节数变小”误认为网络总量消失。

## 7. 从 Gbps 手算传输时间

网络通常用 Gbit/s， Tensor 大小常用 GiB。 第一步先除以 8。 200 Gbit/s 的十进制理论带宽：

\[
200/8=25\text{ GB/s}
\]

传 1 GiB，即 \(1.0737\) GB， 忽略所有开销时：

\[
1.0737/25=0.04295\text{ s}\approx43\text{ ms}
\]

若端到端有效利用率只有 70%：

\[
25\times70\%=17.5\text{ GB/s}
\]

\[
1.0737/17.5\approx61\text{ ms}
\]

这 61 ms 若完全串行， 会直接进入 TTFT 到首个 Decode token 的临界路径。

## 8. 带宽预算不是只看单请求

记平均 Prompt KV 大小为 \(\bar K\)， 到达率为 \(\lambda\) 请求/s。 最低平均数据率近似为：

\[
BW_{demand}=\lambda \times \bar K
\]

若 8K 请求平均 1 GiB， 到达率为 4 req/s：

\[
BW_{demand}=4\text{ GiB/s}\approx34.4\text{ Gbit/s}
\]

平均值还不够。 真实网络要为突发、协议开销、重试和其他业务留余量。 若目标链路长期跑到 90% 以上， 排队会放大传输尾延迟。 容量规划通常应以测得的有效带宽和目标 P99 为准， 而不是把标称线速全部售出。

## 9. 传输能否被隐藏

设 Prefill 剩余计算时间为 \(T_c\)， KV 传输时间为 \(T_x\)。 若完全串行：

\[
T_{handoff}=T_c+T_x
\]

若按层或 block 流水且理想重叠：

\[
T_{handoff}\approx\max(T_c,T_x)+T_{sync}
\]

这里的 \(T_{sync}\) 是同步、注册与尾部屏障。 实际重叠率受以下条件影响：

- 源 KV 是否已完成写入。
- 传输是否真正异步。
- copy engine、NIC 与计算是否争用。
- D 是否能按块消费，还是必须等待全部到齐。
- Connector 是否支持分层或 block 粒度完成通知。

“异步 API 返回了”不代表数据已到达。

## 10. Connector 到底是什么

在 vLLM 语境中， KV Connector 是引擎与外部 KV 数据移动/存储机制之间的抽象。 它需要参与：

- 查询哪些 token 的 KV 可复用。
- 为发送或加载安排 blocks。
- 导出/交换内存元数据。
- 发起异步保存与加载。
- 告知调度器 KV 是否就绪。
- 在请求结束或失败时回收状态。

Connector 不等同于某一张网卡， 也不等同于集群路由器。

## 11. 三层不要混为一谈

### 11.1 引擎集成层

例如 vLLM 的 `KVConnector` 接口。 它理解请求、token、KV blocks 和调度时机。

### 11.2 数据移动层

例如 NIXL、NCCL、UCX。 它们负责把字节或 Tensor 从源内存送到目标内存。

### 11.3 存储与复用层

例如 LMCache 或远端 KV 存储。 它们可能把 KV 放在 CPU、NVMe 或远端层级， 支持跨请求或跨实例复用。 一种 Connector 可以调用某个传输库， 也可以接入存储系统。 名称相似不代表职责相同。

## 12. 常见 Connector 认识

### NixlConnector

vLLM 官方文档把它描述为使用 NIXL 的高性能异步 KV 传输 Connector。 NIXL 使用可插拔 backend， 常见默认传输是 UCX。 它适合需要 RDMA、跨进程异步传输和多种内存后端的场景。 部署仍需正确配置网卡、UCX、内存注册和 side channel。

### P2pNcclConnector

它使用 NCCL 路径进行 GPU 间 P2P 数据移动。 官方仓库提供教学示例。 NCCL 能传 Tensor， 但生产系统仍要补充工作进程发现、请求配对、重试和代理高可用。

### LMCacheConnectorV1

它把 vLLM 接到 LMCache， 可借助 NIXL 等路径移动 KV， 并扩展到分层缓存与复用场景。 引入缓存层后， 还要考虑容量、淘汰、命中一致性和多租户隔离。

### Mooncake 等第三方 Connector

vLLM 官方页面列出多种 Connector。 支持列表和参数会随版本变化。 应以已安装版本的文档、源码和兼容矩阵为准。

## 13. NIXL 最小核实路径

安装后先核对版本：

```bash
python - <<'PY'
import importlib.metadata as m
for name in ("vllm", "nixl"):
    try:
        print(name, m.version(name))
    except m.PackageNotFoundError:
        print(name, "not installed")
PY
```

检查 RDMA 设备：

```bash
ibv_devices
ibv_devinfo
```

检查 GPU 与拓扑：

```bash
nvidia-smi topo -m
```

检查当前 vLLM 参数：

```bash
vllm serve --help | rg "kv-transfer|kv_connector|kv_role"
```

不要只跑 `iperf3` 就宣布 KV 带宽达标。 它测的是主机网络路径， 不一定覆盖 GPU 内存注册、PCIe、GPUDirect 与 Connector 协议。

## 14. 端到端带宽实验骨架

至少测三层：

1. NIC 基线：网络工具测主机到主机。
2. GPU 通信基线：对应 NCCL/UCX/NIXL benchmark。
3. 真实 KV：用目标模型与 Prompt 长度测 handoff。

记录：
- Tensor 字节数。
- 注册与元数据交换耗时。
- 纯传输耗时。
- P 完成到 D 首步开始的 handoff gap。
- 平均、P95、P99 有效 GB/s。
- 并发传输数。
有效带宽计算：
\[
BW_{effective}=\frac{Bytes_{completed}}{T_{last}-T_{first}}
\]
务必标明 GB/s 还是 GiB/s， 否则容易产生约 7% 的单位误差。
## 15. 多轮对话的特殊问题
第一轮结束后， D 已持有 Prompt KV 和新生成 token 的 KV。 下一轮若仍让 P 从头计算完整历史， 会重复计算并重复传输。 可选策略包括：
- 会话粘性，把下一轮路由到持有 KV 的节点。
- D→P 双向 KV 传输。
- 外部共享 KV 层。
- 直接重算较短历史。
vLLM 的 NixlConnector 文档提供双向 KV 传输选项， 并设置小规模远端 KV 时优先重算的阈值思想。 是否启用应由历史长度、网络和命中率决定。
## 16. 安全性与正确性边界
KV Cache 是用户输入和生成状态的派生表示， 应按敏感数据处理。 生产环境要考虑：
- 传输网络隔离与访问控制。
- 租户间 block 不得串用。
- 请求 ID 不可碰撞。
- 释放前后避免 use-after-free。
- 模型 revision、KV dtype、block size 必须匹配。
- 超时后的迟到写入不能污染已复用内存。
Connector 的性能测试通过， 不等于这些正确性条件自动满足。
## 📝 总结
1. KV 大小由层数、KV Head、Head Dimension、dtype 和 token 数共同决定。
2. Llama 3.1 8B 类 GQA 配置下，BF16 的 8K Prompt KV 主体约 1 GiB。
3. 200 Gbit/s 链路传 1 GiB 的理想时间约 43 ms，实际需按有效带宽重算。
4. TP 可分摊每 rank 数据，但不会消除聚合字节数。
5. Connector 是引擎集成抽象，不等同于 NIXL/NCCL，也不等同于路由器。
6. 生产验证必须从网络基线走到真实 KV handoff，并看 P99。
## 🎯 自我检验
- [ ] 我会从 `config.json` 找到层数、KV Heads 和 Head Dimension。
- [ ] 我能手算 BF16 与 FP8 KV 大小。
- [ ] 我能把 Gbit/s 正确换成 GB/s 和传输毫秒。
- [ ] 我能解释单 rank 与聚合传输量的区别。
- [ ] 我能区分 Connector、传输 backend、KV 存储和路由器。
- [ ] 我知道异步、零拷贝、GPUDirect 都需要实测验证。
- [ ] 我能列出多轮对话至少三种 KV 策略。
## 📚 论文与官方参考
- [vLLM：Disaggregated Prefilling](https://docs.vllm.ai/en/stable/features/disagg_prefill/)
- [vLLM：NixlConnector Usage Guide](https://docs.vllm.ai/en/stable/features/nixl_connector_usage/)
- [NVIDIA NIXL 官方仓库](https://github.com/ai-dynamo/nixl)
- [NVIDIA：Enhancing Distributed Inference Performance with NIXL](https://developer.nvidia.com/blog/enhancing-distributed-inference-performance-with-the-nvidia-inference-transfer-library/)
- [LMCache：Disaggregated Prefill Example](https://docs.lmcache.ai/getting_started/quickstart/disaggregated_prefill.html)
- [DistServe（网络与放置分析）](https://www.usenix.org/conference/osdi24/presentation/zhong-yinmin)
