---
title: "7.3 KV Cache 传输与 Connector：搬走一座临时仓库"
description: "手算 GQA 模型 KV Cache 大小与带宽需求，理解 Connector、NIXL、NCCL、LMCache 和传输边界"
pubDate: 2026-08-12
category: "inference-optimization"
chapter: 7
order: 703
tags: ["KV Cache", "KV Connector", "NIXL", "NCCL", "LMCache", "RDMA"]
---

## 📑 本节目标

P/D 解耦最容易被低估的一步， 是把 Prefill 产生的 KV Cache 送到 Decode。 它不是几十字节的请求元数据， 而可能是每个请求数百 MiB 甚至数 GiB 的 Tensor。 本节完成四件事：

- 从模型配置推导 KV 字节数。
- 把网络标称 Gbps 换成传输毫秒。
- 区分 Connector、传输后端和 KV 存储层。
- 建立可核实的带宽测试与故障检查方法。

## 1. 类比：不是递交票据，而是搬仓库

Prefill 结束时， 首 token 只是柜台上的一张提货单。 真正让 Decode 能继续工作的， 是每一层、每个 Prompt token 对应的 K 和 V。 若把请求看成搬家：

- 请求 ID 是快递单号。
- block table 是箱子清单。
- KV Tensor 是箱子里的物品。
- Connector 是物流接口。
- UCX/NCCL 等是公路或铁路。
- HBM、DRAM、NVMe 是仓库。

只优化“下单 API”， 并不会让一吨货物瞬间到达。

## 2. KV Cache 大小公式

先考虑常见的 Transformer 自注意力。 每个 token、每一层都保存 Key 和 Value。 记：

- \(L\)：Transformer 层数。
- \(H_{kv}\)：KV Head 数。
- \(D_h\)：每个 Head 的维度。
- \(S\)：每个元素字节数。
- \(T\)：需要传输的 token 数。
- 因子 2：分别对应 K 和 V。

则总 KV 大小近似为：

\[
Bytes_{KV}=2 \times L \times H_{kv} \times D_h \times S \times T
\]

每 token 大小为：

\[
Bytes_{token}=2 \times L \times H_{kv} \times D_h \times S
\]

这是数据主体的近似值， 未包含 block 对齐、元数据、传输协议缓冲和临时副本。

## 3. MHA、GQA 与 MQA 的区别

在传统 Multi-Head Attention 中， \(H_{kv}=H_q\)。 在 Grouped-Query Attention 中， 多个 Query Head 共享一组 K/V， 所以 \(H_{kv}<H_q\)。 在 Multi-Query Attention 中， \(H_{kv}\) 甚至可以为 1。 因此不能用 `num_attention_heads` 直接计算所有模型。 应读取配置里的 `num_key_value_heads`。 可核实命令：

```bash
python - <<'PY'
from transformers import AutoConfig

model = "meta-llama/Llama-3.1-8B-Instruct"
c = AutoConfig.from_pretrained(model)
kv_heads = getattr(c, "num_key_value_heads", c.num_attention_heads)
head_dim = getattr(c, "head_dim", c.hidden_size // c.num_attention_heads)
print({
    "layers": c.num_hidden_layers,
    "attention_heads": c.num_attention_heads,
    "kv_heads": kv_heads,
    "head_dim": head_dim,
})
PY
```

模型仓库可能执行自定义配置代码。 对不可信仓库，不要轻易增加 `trust_remote_code=True`。

## 4. 手算：Llama 3.1 8B 的 8K Prompt

采用公开配置中的典型参数：

- 层数 \(L=32\)。
- KV Head 数 \(H_{kv}=8\)。
- Head Dimension \(D_h=128\)。
- BF16/FP16 元素 \(S=2\) bytes。

每 token：

\[
2 \times 32 \times 8 \times 128 \times 2 =131072\text{ bytes} =128\text{ KiB}
\]

8K 若按 8192 token：

\[
128\text{ KiB}\times8192 =1,073,741,824\text{ bytes} =1\text{ GiB}
\]

也就是说， 一个 8K Prompt 的 KV 主体约 1 GiB。 若是 32K Prompt：

\[
1\text{ GiB}\times4=4\text{ GiB}
\]

这还只是一个请求。 每秒进入 10 个 8K Prompt， 数据生成率近似就是 10 GiB/s。

## 5. KV 量化怎样影响传输

若 KV dtype 从 2 bytes 降到 1 byte， 主体大小理论上减半。 同一例子中：

- BF16/FP16：8K Prompt 约 1 GiB。
- FP8 KV：8K Prompt 约 0.5 GiB。

但必须同时确认：

- P 和 D 是否使用相同 KV dtype。
- Connector 是否支持该 dtype。
- 目标 Attention backend 是否支持。
- 量化 scale 是否需要额外传输。
- 精度是否通过目标任务评测。

“网络省一半”不能跳过兼容性和质量验证。

## 6. Tensor Parallel 下怎么算

若 KV heads 在 \(TP\) 个 rank 间均匀切分， 每个 rank 持有的数据近似是总量的 \(1/TP\)。 但所有 rank 的聚合传输量仍接近总 KV 大小。 例如 1 GiB KV、TP=4：

- 每个 rank 约发送 256 MiB。
- 四个 rank 合计约 1 GiB。

若四张 GPU 各有独立、无争用的链路， 传输可并行。 若最终共享一个 NIC 或 PCIe 上行， 瓶颈仍是共享链路的聚合带宽。 不要把“单 rank 字节数变小”误认为网络总量消失。

## 7. 从 Gbps 手算传输时间

网络通常用 Gbit/s， Tensor 大小常用 GiB。 第一步先除以 8。 200 Gbit/s 的十进制理论带宽：

\[
200/8=25\text{ GB/s}
\]

传 1 GiB，即 \(1.0737\) GB， 忽略所有开销时：

\[
1.0737/25=0.04295\text{ s}\approx43\text{ ms}
\]

若端到端有效利用率只有 70%：

\[
25\times70\%=17.5\text{ GB/s}
\]

\[
1.0737/17.5\approx61\text{ ms}
\]

这 61 ms 若完全串行， 会直接进入 TTFT 到首个 Decode token 的临界路径。

## 8. 带宽预算不是只看单请求

记平均 Prompt KV 大小为 \(\bar K\)， 到达率为 \(\lambda\) 请求/s。 最低平均数据率近似为：

\[
BW_{demand}=\lambda \times \bar K
\]

若 8K 请求平均 1 GiB， 到达率为 4 req/s：

\[
BW_{demand}=4\text{ GiB/s}\approx34.4\text{ Gbit/s}
\]

平均值还不够。 真实网络要为突发、协议开销、重试和其他业务留余量。 若目标链路长期跑到 90% 以上， 排队会放大传输尾延迟。 容量规划通常应以测得的有效带宽和目标 P99 为准， 而不是把标称线速全部售出。

## 9. 传输能否被隐藏

设 Prefill 剩余计算时间为 \(T_c\)， KV 传输时间为 \(T_x\)。 若完全串行，总墙钟为：

\[
T_{wall}=T_c+T_x
\]

若按层或 block 流水且理想重叠：

\[
T_{wall}\approx\max(T_c,T_x)+T_{sync}
\]

额外 handoff 是 \(T_{handoff,extra}=T_{wall}-T_c\)。这里的 \(T_{sync}\) 是同步、注册与尾部屏障。 实际重叠率受以下条件影响：

- 源 KV 是否已完成写入。
- 传输是否真正异步。
- copy engine、NIC 与计算是否争用。
- D 是否能按块消费，还是必须等待全部到齐。
- Connector 是否支持分层或 block 粒度完成通知。

“异步 API 返回了”不代表数据已到达。

## 10. Connector 到底是什么

在 vLLM 语境中， KV Connector 是引擎与外部 KV 数据移动/存储机制之间的抽象。 它需要参与：

- 查询哪些 token 的 KV 可复用。
- 为发送或加载安排 blocks。
- 导出/交换内存元数据。
- 发起异步保存与加载。
- 告知调度器 KV 是否就绪。
- 在请求结束或失败时回收状态。

Connector 不等同于某一张网卡， 也不等同于集群路由器。

## 11. 三层不要混为一谈

### 11.1 引擎集成层

例如 vLLM 的 `KVConnector` 接口。 它理解请求、token、KV blocks 和调度时机。

### 11.2 数据移动层

例如 NIXL、NCCL、UCX。 它们负责把字节或 Tensor 从源内存送到目标内存。

### 11.3 存储与复用层

例如 LMCache 或远端 KV 存储。 它们可能把 KV 放在 CPU、NVMe 或远端层级， 支持跨请求或跨实例复用。 一种 Connector 可以调用某个传输库， 也可以接入存储系统。 名称相似不代表职责相同。

## 12. 常见 Connector 认识

### NixlConnector

vLLM 官方文档把它描述为使用 NIXL 的高性能异步 KV 传输 Connector。 NIXL 使用可插拔 backend， 常见默认传输是 UCX。 它适合需要 RDMA、跨进程异步传输和多种内存后端的场景。 部署仍需正确配置网卡、UCX、内存注册和 side channel。

### P2pNcclConnector

它使用 NCCL 路径进行 GPU 间 P2P 数据移动。 官方仓库提供教学示例。 NCCL 能传 Tensor， 但生产系统仍要补充工作进程发现、请求配对、重试和代理高可用。

### LMCacheConnectorV1

它把 vLLM 接到 LMCache， 可借助 NIXL 等路径移动 KV， 并扩展到分层缓存与复用场景。 引入缓存层后， 还要考虑容量、淘汰、命中一致性和多租户隔离。

### Mooncake 等第三方 Connector

vLLM 官方页面列出多种 Connector。 支持列表和参数会随版本变化。 应以已安装版本的文档、源码和兼容矩阵为准。

## 13. NIXL 最小核实路径

安装后先核对版本：

```bash
python - <<'PY'
import importlib.metadata as m
for name in ("vllm", "nixl"):
    try:
        print(name, m.version(name))
    except m.PackageNotFoundError:
        print(name, "not installed")
PY
```

检查 RDMA 设备：

```bash
ibv_devices
ibv_devinfo
```

检查 GPU 与拓扑：

```bash
nvidia-smi topo -m
```

检查当前 vLLM 参数：

```bash
vllm serve --help | rg "kv-transfer|kv_connector|kv_role"
```

不要只跑 `iperf3` 就宣布 KV 带宽达标。 它测的是主机网络路径， 不一定覆盖 GPU 内存注册、PCIe、GPUDirect 与 Connector 协议。

## 14. 端到端带宽实验骨架

至少测三层：

1. NIC 基线：网络工具测主机到主机。
2. GPU 通信基线：对应 NCCL/UCX/NIXL benchmark。
3. 真实 KV：用目标模型与 Prompt 长度测 handoff。

记录：
- Tensor 字节数。
- 注册与元数据交换耗时。
- 纯传输耗时。
- P 完成到 D 首步开始的 handoff gap。
- 平均、P95、P99 有效 GB/s。
- 并发传输数。
有效带宽计算：
\[
BW_{effective}=\frac{Bytes_{completed}}{T_{last}-T_{first}}
\]
务必标明 GB/s 还是 GiB/s， 否则容易产生约 7% 的单位误差。
## 15. 多轮对话的特殊问题
第一轮结束后， D 已持有 Prompt KV 和新生成 token 的 KV。 下一轮若仍让 P 从头计算完整历史， 会重复计算并重复传输。 可选策略包括：
- 会话粘性，把下一轮路由到持有 KV 的节点。
- D→P 双向 KV 传输。
- 外部共享 KV 层。
- 直接重算较短历史。
vLLM 的 NixlConnector 文档提供双向 KV 传输选项， 并设置小规模远端 KV 时优先重算的阈值思想。 是否启用应由历史长度、网络和命中率决定。
## 16. 安全性与正确性边界
KV Cache 是用户输入和生成状态的派生表示， 应按敏感数据处理。 生产环境要考虑：
- 传输网络隔离与访问控制。
- 租户间 block 不得串用。
- 请求 ID 不可碰撞。
- 释放前后避免 use-after-free。
- 模型 revision、KV dtype、block size 必须匹配。
- 超时后的迟到写入不能污染已复用内存。
Connector 的性能测试通过， 不等于这些正确性条件自动满足。

## 17. 从逻辑 Tensor 到物理 Layout

KV 大小公式只回答“有多少元素”，Connector 还必须回答“元素在内存里怎样排”。常见逻辑维度包含：

```text
layer
K or V
block
token within block
kv head
head dimension
```

实际后端可能使用：

```text
NHD: [num_tokens, num_kv_heads, head_dim]
HND: [num_kv_heads, num_tokens, head_dim]
```

再叠加 layer、K/V 和 block 维度。只要 P/D layout 不同，即使字节数完全一致，也不能把源地址直接复制到目标地址后解释为同一 Tensor。

### 17.1 一个 block 的主体大小

记 block token 数为 \(B\)，则每层一个请求块中 K+V 主体大小为：

\[
Bytes_{block,layer}
=2\times B\times H_{kv}\times D_h\times S
\]

仍用 Llama 3.1 8B 类参数，\(B=16\)：

\[
2\times16\times8\times128\times2
=65536\text{ bytes}=64\text{ KiB}
\]

跨 32 层：

\[
64\text{ KiB}\times32=2\text{ MiB/block}
\]

8192-token Prompt 需要：

\[
N_{blocks}=\lceil8192/16\rceil=512
\]

\[
512\times2\text{ MiB}=1\text{ GiB}
\]

这与逐 token 公式一致，说明推导闭合。

### 17.2 非整块对齐

若 Prompt 是 8200 token：

\[
N_{blocks}=\lceil8200/16\rceil=513
\]

物理分配按完整 block 计：

\[
513\times2\text{ MiB}=1026\text{ MiB}
\]

有效 token 主体为：

\[
8200\times128\text{ KiB}=1025\text{ MiB}
\]

这里尾块浪费约 1 MiB。单请求看似不大，但大量短请求时，最后一个 block 的相对浪费更明显。

### 17.3 Descriptor 数为什么可能很大

若每层、K/V、每个 block 都是独立内存段，最朴素 descriptor 数可到：

\[
N_{desc}=L\times2\times N_{blocks}
\]

本例：

\[
32\times2\times512=32768
\]

实际引擎和 Connector 会聚合连续段，否则元数据、注册和 posting 成本会过高。若启用跨层连续 block，一整个逻辑 block 可映成更少物理段，descriptor 数显著下降。

vLLM v0.27.1 的 NixlConnector 文档提供实验性的 `enable_cross_layers_blocks`；它要求 Attention backend 支持，不能仅因参数存在就默认可用。

### 17.4 TP Layout

设 P 使用 \(TP_P=4\)，D 使用 \(TP_D=2\)。即使总 KV 都是 1 GiB：

```text
P: 4 ranks × 256 MiB
D: 2 ranks × 512 MiB
```

传输不只是四个源 rank 对两个目标 rank 的字节搬运，还要定义：

- KV head 在 rank 间怎样切分。
- 哪些源分片拼到同一目标分片。
- block ID 是否全局还是 rank-local。
- TP/PP rank 到 side-channel endpoint 的映射。
- collective 或点对点操作的顺序。

若 Connector 不支持异构 TP，配置可以启动却无法正确交接。兼容矩阵必须是部署前置条件。

### 17.5 HND↔NHD 不是免费转换

vLLM v0.27.1 文档列出实验性的 heterogeneous KV layout：

```text
Prefill: HND
Decode:  NHD
```

可通过 `enable_permute_local_kv` 触发布局转换。转换成本近似至少读写一次 KV：

\[
Bytes_{permute,traffic}\gtrsim2\times Bytes_{KV}
\]

1 GiB KV 至少形成约 2 GiB HBM 流量，还可能需要临时 buffer 和额外 Kernel。网络字节未变，但 handoff 临界路径增加，因此要单独测 `permute + transfer`。

## 18. 完整 handoff 时间模型

只用 \(K/BW\) 会漏掉固定成本。更完整地写：

\[
T_{handoff}=
T_{lookup}
+T_{alloc}
+T_{meta}
+T_{register}
+T_{post}
+T_{wire}
+T_{complete}
+T_{commit}
+T_{queue,D}
\]

含义：

- \(T_{lookup}\)：查询可用远端 KV、命中 token 和请求状态。
- \(T_{alloc}\)：D 分配目标 blocks。
- \(T_{meta}\)：交换 engine ID、地址、rkey、block IDs、dtype/layout。
- \(T_{register}\)：内存注册或查找已注册 region。
- \(T_{post}\)：构建 descriptor 并提交异步操作。
- \(T_{wire}\)：数据真实经过 NVLink/PCIe/NIC。
- \(T_{complete}\)：轮询 CQ、事件或 future。
- \(T_{commit}\)：发布“目标 KV 可见”的完成标记。
- \(T_{queue,D}\)：就绪后等待第一个 Decode iteration。

### 18.1 冷路径与热路径

冷路径可能为每次 transfer 做注册：

\[
T_{cold}=T_{reg,src}+T_{reg,dst}+T_{meta}+T_{copy}+T_{sync}
\]

热路径复用已注册的 HBM region：

\[
T_{hot}=T_{meta,lookup}+T_{copy}+T_{sync}
\]

如果压测只在连接和注册预热后测热路径，应明确标注；故障恢复、扩容和新 worker 启动时仍会走冷路径。

### 18.2 注册为什么不能忽略

RDMA/GPUDirect 路径通常需要把内存 region 注册给传输后端，生成远端可访问的 key/handle。注册涉及：

- 页表 pin 或 VMM 映射。
- GPU VA 到传输层 descriptor 的转换。
- NIC/driver 资源。
- region 元数据分发。

对 1 GiB 大 payload，copy 占主导；对大量小 block，descriptor 和注册固定成本可能占主导。应该比较：

\[
\frac{T_{register}+T_{post}}{T_{handoff}}
\]

若超过 20%，继续升级链路线速未必显著降低尾延迟。

### 18.3 并发传输与排队

单流有效带宽为 \(B_1\)，不代表 \(n\) 个并发请求各得到 \(B_1\)。共享 NIC 总带宽 \(B_{NIC}\) 时：

\[
\sum_{i=1}^{n}B_i\le B_{NIC}
\]

若 4 个 1 GiB transfer 同时共享有效 20 GiB/s 链路，公平近似下每个 5 GiB/s：

\[
T_i\approx1/5=0.2\text{ s}
\]

而串行每个只需 50 ms。并发提高聚合吞吐，却把单请求 TTFT 拉长；调度器需要限制 in-flight transfer 或按 SLO 排队。

## 19. NixlConnector 生命周期

下面按 vLLM v0.27.1 的 pull 模式理解，不把 NIXL 简化成“调用一次 RDMA”。

### 19.1 进程启动

```text
vLLM worker 启动
  -> 解析 KVTransferConfig
  -> 创建 NixlConnector
  -> 初始化 NIXL agent/backend（默认常见为 UCX）
  -> 选择/注册 KV buffer
  -> 启动 side channel
  -> 发布 engine_id、host、port、rank 信息
```

每个 worker 需要唯一 side-channel 端口；不同主机可以复用同一端口号。TP/DP 下不能只给整个实例一个模糊端口。

### 19.2 P 完成 Prefill

```text
Scheduler 为请求生成 KV blocks
  -> Connector 收集 block IDs 与内存 descriptors
  -> 构造 kv_transfer_params
  -> 设置源 KV lease
  -> HTTP 响应把参数返回 Router
```

源 blocks 此时不能立刻回收。v0.27.1 文档中的 `kv_lease_duration` 默认 30 秒；D 排队期间 heartbeat 可延长 lease，成功 read notification 或超时负责释放。

### 19.3 Router 转发元数据

Router 向 D 发送原请求与：

```text
remote_engine_id
remote_request_id
remote_block_ids
remote_host
remote_port
TP/PP/DP mapping（按拓扑需要）
```

这些是控制面元数据，不是 KV 本身。Router 不应代理 1 GiB Tensor 数据。

### 19.4 D 分配并发起 READ

```text
D Scheduler 识别 do_remote_prefill
  -> 分配本地目标 blocks
  -> Connector 通过 side channel 取得/校验远端描述符
  -> 构建 scatter-gather descriptors
  -> post NIXL READ
  -> 异步等待 backend completion
```

Pull 模式让 D 在有本地容量时才读取。`post` 返回只表示工作已提交，不表示数据可被 Attention 使用。

### 19.5 完成、通知与提交

```text
backend CQ/future 完成
  -> D 校验所有 descriptor
  -> send notification 给 P
  -> D 将目标 blocks 标记 READY
  -> Scheduler 允许请求进入 Decode
  -> P 收到通知后释放/缩短源 lease
```

`vllm:nixl_num_failed_notifications` 增长时，数据可能已经到 D，但 P 未收到完成通知，导致源 KV 滞留。它和 transfer failure 是不同故障。

### 19.6 取消与超时

取消可能发生在：

```text
P 尚未完成
P 已完成、D 未开始 read
read 已 post
数据完成、notification 丢失
D 已 Decode
```

每个阶段都应以 `(request_id, generation)` 做幂等清理。仅用可复用 request ID 而无 generation，迟到 completion 可能把新请求的 block 错标为 READY。

## 20. NCCL Connector 生命周期与语义差异

NCCL 擅长 GPU collective/point-to-point 通信，但其常见编程语义与 one-sided RDMA pull 不同。

### 20.1 建组

```text
发现 P/D ranks
  -> 交换 NCCL unique ID
  -> 每个 rank 创建 communicator
  -> 建立 peer/channel 拓扑
```

建组是高固定成本，必须在请求热路径外复用。worker 重启或 rank 变化通常需要重建 communicator。

### 20.2 匹配发送与接收

点对点路径需要 P/D 对同一请求、相同 dtype/count、相容顺序执行 send/recv：

```text
P rank 0 ncclSend(req A shard 0)
D rank 0 ncclRecv(req A shard 0)
...
CUDA event 记录 stream 完成
```

如果两侧请求顺序不同，可能等待错误 peer 操作甚至挂起。生产控制面要把 `request_id -> operation sequence` 明确化。

### 20.3 Stream 同步

NCCL API 通常把工作 enqueue 到 CUDA stream。CPU 调用返回后：

- 源 buffer 在发送完成前不能复用。
- 目标 buffer 在接收 stream event 完成前不能被 Attention 读取。
- 跨 stream 消费要用 event dependency。

因此：

\[
T_{NCCL}=T_{enqueue}+T_{transport}+T_{cuda\ event}+T_{commit}
\]

### 20.4 故障域

communicator 中一个 rank 卡住可能影响整组。需要：

- operation timeout/watchdog。
- communicator abort。
- worker 级重建。
- 未完成请求的 fail 或重新 Prefill。

NCCL 能搬 Tensor，不提供 Router、lease、KV 命中查询和跨请求缓存语义。

## 21. LMCacheConnectorV1 生命周期

LMCache 更接近“KV 存储/复用系统 + vLLM Connector”，而不是单纯一条网络通道。

### 21.1 保存路径

```text
vLLM Scheduler 产生 KV
  -> LMCacheConnectorV1 选择可保存 token/block
  -> 序列化/映射 KV layout
  -> 写入 local CPU / remote memory / storage tier
  -> 发布 cache key 与完成状态
```

底层可以使用 NIXL 等数据移动路径，但 LMCache 还负责命名、分层、容量和复用。

### 21.2 加载路径

```text
新请求到达
  -> 根据模型、token prefix、dtype/layout 构造 lookup key
  -> 返回命中 token 范围
  -> Scheduler 为命中部分分配 blocks
  -> 异步 load
  -> 完成后标记命中 blocks 可见
  -> 只 Prefill miss suffix
```

与一次 P→D handoff 相比，它还要处理跨请求持久命中和淘汰。

### 21.3 额外状态

```text
cache_key / model_revision
token_hash or prefix identity
location tier
owner tenant
TTL / last_access
eviction state
inflight readers/writers
layout / dtype / block size
```

如果 cache entry 在 load 中被淘汰，必须用引用计数、lease 或 copy-on-read 防止 use-after-free。

### 21.4 何时选 LMCache

适合：

- P/D handoff 之外还需要跨请求 Prefix 复用。
- 需要 HBM→DRAM→NVMe/远端的分层容量。
- 多轮或共享系统提示带来高命中率。

不适合仅凭“可能会命中”就引入。若命中率低，lookup、元数据和分层搬运只会增加 TTFT。

## 22. Connector 对照的正确问法

不要问“NIXL、NCCL、LMCache 谁最快”，应问：

```text
我的语义是一次 P→D handoff，还是跨请求持久复用？
链路是 NVLink、PCIe、RoCE/IB、TCP 还是存储？
需要 one-sided pull 还是两侧匹配 send/recv？
能否复用注册 region / communicator？
P/D TP 和 KV layout 是否相同？
谁持有 lease、TTL 和完成标记？
失败后 fail、recompute 还是从外部缓存恢复？
```

NIXL、NCCL、LMCache 覆盖的层次有重叠，但不是互换标签。

## 23. 完整带宽与同步手算

假设：

```text
KV payload             1 GiB
有效链路                17.5 GB/s
descriptor 构建         1.5 ms
热注册查找              0.5 ms
side-channel 元数据     1.0 ms
post                    0.8 ms
completion + notify     2.2 ms
layout permute          4.0 ms
D queue                 6.0 ms
```

线速部分：

\[
T_{wire}=1.0737/17.5\approx61.4\text{ ms}
\]

完整 handoff：

\[
T_{handoff}
=1.5+0.5+1.0+0.8+61.4+2.2+4.0+6.0
=77.4\text{ ms}
\]

若只报告 `KVBytes / link bandwidth=61.4 ms`，低估了：

\[
77.4-61.4=16.0\text{ ms}
\]

若 TTFT SLO 是 800 ms，这 16 ms 也许可接受；若 handoff budget 只有 50 ms，则单靠 200 Gbit/s 链路已不满足。

### 23.1 流水重叠手算

假设 Prefill 还有 90 ms，数据传输 61.4 ms，80% 数据时间可与 Prefill 重叠，固定尾部 7 ms：

\[
T_{overlap}=0.8\times\min(90,61.4)=49.12\text{ ms}
\]

\[
T_{wall}
=T_P+T_{xfer}-T_{overlap}+T_{fixed}
=90+61.4-49.12+7
=109.28\text{ ms}
\]

相对 90 ms Prefill 基线，额外 handoff 为：

\[
T_{handoff,extra}=T_{wall}-T_P=19.28\text{ ms}
\]

总墙钟是 109.28 ms；额外 handoff 才是流水重叠后对临界路径新增的 19.28 ms。

## 24. 实验矩阵：逐层定位开销

### 24.1 四级基线

1. **Host network**
   - 验证 MTU、路由、丢包和主机线速。
2. **GPU transport**
   - 验证 GPU memory 到 GPU memory。
3. **Connector synthetic**
   - 固定 payload 与 descriptor 数。
4. **真实模型 handoff**
   - 带 block table、Scheduler 和 Decode 接管。

### 24.2 扫描维度

```text
payload: 2 MiB, 16 MiB, 128 MiB, 1 GiB, 4 GiB
concurrency: 1, 2, 4, 8
descriptors: low, medium, high
path: same-host, same-rack, cross-rack
registration: cold, warm
layout: same, permute
```

### 24.3 必记指标

vLLM v0.27.1 NixlConnector 暴露：

```text
vllm:nixl_xfer_time_seconds
vllm:nixl_post_time_seconds
vllm:nixl_bytes_transferred
vllm:nixl_num_descriptors
vllm:nixl_num_failed_transfers
vllm:nixl_num_failed_notifications
vllm:nixl_num_kv_expired_reqs
```

周期日志中的 throughput 是区间聚合吞吐，不应当成每请求带宽。`xfer_time` 从 post 到 backend complete，仍不等于 Router 看到的全部 handoff。

## 25. 排障闭环

### `post_time` 高、`xfer_time` 不高

- descriptor 太碎。
- 注册缓存未命中。
- CPU/driver 提交拥塞。
- side-channel 元数据过大。

### `xfer_time` 高、链路利用率低

- UCX 选择了 TCP 而非 RDMA。
- GPUDirect 未生效，发生 host bounce。
- GPU/NIC NUMA 不亲和。
- payload 太小，固定 RTT 占主导。
- 多流拥塞或 PFC/ECN 配置异常。

### failed transfer 为 0，但 expired requests 增长

数据路径未必坏；可能 D 排队超过 P 的 `kv_lease_duration`。对照：

```text
D queue wait P99
lease duration
heartbeat success
read notification
```

### notification failure 增长

D 可能已收到 KV，而 P 仍保留源 blocks。检查 side channel、通知重试和幂等释放；不要直接缩短 TTL，否则可能转成 use-after-free。

### 输出错误但 transfer 成功

字节搬运成功不代表语义正确。核对：

```text
request generation
token/block mapping
K/V order
layer order
HND/NHD
TP shard map
dtype/scale
completion fence
```

### 带宽达标但 TTFT 仍高

用完整公式逐项打点，通常遗漏的是 D queue、layout permute、注册或完成 commit。

## 26. 本节闭环

```text
问题：KV 不是一个连续文件，且传输进入 TTFT 临界路径
  ↓
机制：block/layout/shard 映射 + 注册 + async post + completion fence
  ↓
公式：payload、block、descriptor、wire time 与完整 handoff 分解
  ↓
实现：NIXL pull/lease，NCCL matched stream，LMCache lookup/tier
  ↓
实验：网络→GPU→Connector→真实请求四层扫描
  ↓
排障：区分 post、wire、notify、expire、layout 与 D queue
```

## 📝 总结
1. KV 大小由层数、KV Head、Head Dimension、dtype 和 token 数共同决定。
2. Llama 3.1 8B 类 GQA 配置下，BF16 的 8K Prompt KV 主体约 1 GiB。
3. 200 Gbit/s 链路传 1 GiB 的理想时间约 43 ms，实际需按有效带宽重算。
4. TP 可分摊每 rank 数据，但不会消除聚合字节数。
5. Connector 是引擎集成抽象，不等同于 NIXL/NCCL，也不等同于路由器。
6. 物理 layout、TP shard、descriptor、注册与完成屏障都会让实际 handoff 高于 `KV/BW`。
7. NIXL、NCCL、LMCache 分别偏向异步数据移动、匹配通信和分层复用，生命周期与故障语义不同。
8. 生产验证必须从网络基线走到真实 KV handoff，并看 P99。
## 🎯 自我检验
- [ ] 我会从 `config.json` 找到层数、KV Heads 和 Head Dimension。
- [ ] 我能手算 BF16 与 FP8 KV 大小。
- [ ] 我能把 Gbit/s 正确换成 GB/s 和传输毫秒。
- [ ] 我能解释单 rank 与聚合传输量的区别。
- [ ] 我能区分 Connector、传输 backend、KV 存储和路由器。
- [ ] 我知道异步、零拷贝、GPUDirect 都需要实测验证。
- [ ] 我能列出多轮对话至少三种 KV 策略。
## 📚 论文与官方参考
- [vLLM：Disaggregated Prefilling](https://docs.vllm.ai/en/stable/features/disagg_prefill/)
- [vLLM：NixlConnector Usage Guide](https://docs.vllm.ai/en/stable/features/nixl_connector_usage/)
- [NVIDIA NIXL 官方仓库](https://github.com/ai-dynamo/nixl)
- [NVIDIA：Enhancing Distributed Inference Performance with NIXL](https://developer.nvidia.com/blog/enhancing-distributed-inference-performance-with-the-nvidia-inference-transfer-library/)
- [LMCache：Disaggregated Prefill Example](https://docs.lmcache.ai/getting_started/quickstart/disaggregated_prefill.html)
- [DistServe（网络与放置分析）](https://www.usenix.org/conference/osdi24/presentation/zhong-yinmin)
